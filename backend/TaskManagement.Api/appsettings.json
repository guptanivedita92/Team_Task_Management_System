{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "_comment": "Set via environment variable ConnectionStrings__DefaultConnection or dotnet user-secrets in development. Do NOT put real credentials here.",
    "DefaultConnection": ""
  },

  "Jwt": {
    "_comment": "Issuer/Audience are fine to keep here. Key must be supplied via environment variable Jwt__Key or user-secrets - at least 32 characters, never committed.",
    "Issuer": "TaskManagementApi",
    "Audience": "TaskManagementClient",
    "Key": "",
    "ExpiryMinutes": 60
  },

  "Cors": {
    "AllowedOrigins": [ "http://localhost:5173" ]
  }
}
