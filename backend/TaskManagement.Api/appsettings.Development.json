{
  "_comment": "This file is safe to commit because it intentionally has NO secrets. For local development, run the dotnet user-secrets commands in the README instead of editing this file with real values.",
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  }
}
